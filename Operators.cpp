#include <iostream>
using namespace std;

int main()
{

/*

    // Arithmaic Operators

    int a = 6;
    int b = 5;

    cout<<"Addition : "<< a + b <<endl;
    cout<<"Division : "<< a / b <<endl;
    cout<<"Substraction : "<< a - b <<endl;
    cout<<"Multiply : "<< a * b <<endl;

    cout<<endl<<endl;

*/


/*

    // Relational Opaerators(== , != , < , > , <= , >= )

    float num1 = 6.1, num2 = 7.2;

    if (num1 == num2) {
        cout<<"Number are Equal";
    }

    else if (num1 < num2) {
        cout<<"Number1 Less than Number2";
    }
    else if (num1 > num2) {
        cout<<"Number1 greater than Number2";
    }
    else if (num1 != num2) {
        cout<<"Number are not Equal";
    }
    else if (num1 <= num2) {
        cout<<"Number1 Less Equal to Number2";
    }
    else if (num1 >= num2) {
        cout<<"Number1 greater Equal to Number2";
    }

    else {
        cout<<"Unknown";
    }


*/


/*

    // Logical Operator (&& (Logical AND), || (Logical OR), ! (Logical NOT))

    //  Logical AND operator ( Both are true )
    int age1 = 20;
    int age2 = 34;

    if(age1 == 20 && age2 == 34) {
        cout<<"Both are true";
    }
    else {
        cout<<"either one or two is false";
    }

    cout<<endl<<endl;

    //  Logical OR operator ( One are true )

    int age3 = 20;
    int age4 = 34;

    if(age3 == 20 || age4 == 40) {
        cout<<"Both are true";
    }
    else {
        cout<<"either one or two is false";
    }

    cout<<endl<<endl;

    //  Logical NOT operator ( if true then return is false )

    int age5 = 20;
    int age6 = 34;

    if(!(age5 == 20)) {
        cout<<"Both are true";
    }
    else {
        cout<<"either one or two is false";
    }

*/

    cout<<endl<<endl;


    // Assignment Operator (= , += , -= , *= , /= , %= )


    int b1 = 3;
    b1 += 2;  // b1=b1+2

    cout<<"Your value of B1 is : " <<b1<<endl;


}
